# ⚠️ PRODUCTION ENVIRONMENT TEMPLATE
# Copy this file to .env.production and set real values
# NEVER commit .env.production to Git!

# =============================================================================
# 🔐 MONGODB CREDENTIALS (REQUIRED - CHANGE THESE!)
# =============================================================================
MONGO_INITDB_ROOT_USERNAME=rpgarena_admin
MONGO_INITDB_ROOT_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
MONGO_INITDB_DATABASE=RPGArena

# MongoDB user for application
MONGO_USER=rpgarena_user
MONGO_PASSWORD=CHANGE_ME_ANOTHER_STRONG_PASSWORD

# =============================================================================
# 🌐 APPLICATION CONFIGURATION
# =============================================================================
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:5000;https://+:5001

# =============================================================================
# 🔒 SSL/TLS CERTIFICATES
# =============================================================================
# Certificate password (use strong password in production!)
CERTIFICATE_PASSWORD=CHANGE_ME_CERTIFICATE_PASSWORD

# =============================================================================
# 📊 MONGOEXPRESS (ADMIN INTERFACE)
# =============================================================================
# Credentials for MongoExpress web UI
ME_CONFIG_BASICAUTH_USERNAME=CHANGE_ME_ADMIN_USERNAME
ME_CONFIG_BASICAUTH_PASSWORD=CHANGE_ME_ADMIN_PASSWORD

# =============================================================================
# 💾 BACKUP CONFIGURATION
# =============================================================================
BACKUP_RETENTION_DAYS=7
BACKUP_SCHEDULE="0 2 * * *"

# =============================================================================
# 🔐 EXTERNAL SECRETS MANAGEMENT (OPTIONAL)
# =============================================================================
# Uncomment and configure if using Azure Key Vault or AWS Secrets Manager

# Azure Key Vault
# AZURE_KEYVAULT_URL=https://your-keyvault.vault.azure.net/
# AZURE_CLIENT_ID=your-client-id
# AZURE_CLIENT_SECRET=your-client-secret
# AZURE_TENANT_ID=your-tenant-id

# AWS Secrets Manager
# AWS_REGION=eu-west-1
# AWS_SECRET_NAME=rpgarena/production/mongodb

# =============================================================================
# 📝 INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.production.template .env.production
# 2. Edit .env.production with real values
# 3. Set strong passwords (min 20 characters, alphanumeric + special chars)
# 4. Add .env.production to .gitignore (already done)
# 5. Use docker compose with: docker compose --env-file .env.production up -d
#
# Password Generation Examples:
#   openssl rand -base64 32
#   pwgen -s 32 1
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
