namespace RPGArena.CombatEngine;

/// <summary>
/// Point d'entrée central pour initialiser et orchestrer un combat dans RPGArena.
/// Peut être utilisé comme façade pour encapsuler la logique de BattleArena.
/// </summary>
public class CombatEngine
{
    // Cette classe peut servir à encapsuler la logique de BattleManager ou BattleArena si besoin.
    // Pour l'instant, elle reste vide ou minimaliste selon ton architecture.
}